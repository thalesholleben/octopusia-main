# =============================================================================
# OCTOPUS IA - VARIÁVEIS DE AMBIENTE (BACKEND)
# =============================================================================
# Copie este arquivo para .env e preencha com os valores reais
# NUNCA commite o arquivo .env com credenciais reais!
# =============================================================================

# Database (PostgreSQL)
# Formato: postgres://usuario:senha@host:porta/database?sslmode=disable
DATABASE_URL=postgres://usuario:senha_segura@localhost:5432/octopusia?sslmode=disable

# JWT (CRÍTICO: gerar novo secret para produção!)
# Gerar com: openssl rand -base64 64
JWT_SECRET=gerar_um_secret_seguro_com_pelo_menos_64_caracteres_aqui
JWT_EXPIRES_IN=7d

# Registration Security (chave para novos cadastros)
# Gerar com: openssl rand -base64 32
REGISTRATION_KEY=gerar_uma_chave_segura_aqui

# Server
PORT=3001
NODE_ENV=development

# CORS (URL do frontend)
# Desenvolvimento: http://localhost:8080
# Produção: https://seu-dominio.com
FRONTEND_URL=http://localhost:8080

# Password Recovery (opcional - URL do webhook n8n)
# Exemplo: https://seu-n8n.com/webhook/password-recovery
PASSWORD_RECOVERY_WEBHOOK_URL=

# Reports Webhook (OBRIGATÓRIO para sistema de relatórios)
# URL do webhook n8n que processa geração de relatórios com IA
# Exemplo: https://seu-n8n.com/webhook/generate-report
# O webhook recebe: reportId, userId, userEmail, reportType, filters, summary (KPIs)
# Deve retornar callback para: POST /api/internal/reports/callback
REPORTS_WEBHOOK_URL=

# Internal API Key (OBRIGATÓRIO para callbacks do n8n)
# Gerar com: openssl rand -base64 32
# Esta chave autentica requisições de automações internas (n8n, scripts, etc.)
# Usada em rotas /api/internal/* (finance/summary, reports/callback)
INTERNAL_API_KEY=octopus-n8n-secret-key-change-me
