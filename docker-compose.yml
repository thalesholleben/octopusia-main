networks:
  easypanel:
    external: true
  easypanel-n8n:
    external: true

services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    networks:
      - easypanel
      - easypanel-n8n
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_URL: postgres://n8nuser:Vk2%40esEaYS@n8n_postgres:5432/octopusia?sslmode=disable
      JWT_SECRET: TEwh2LEpU4mbIJ3MiMykRY0cspX2d87zqkXnegYENKM
      JWT_EXPIRES_IN: 7d
      FRONTEND_URL: https://hub.thalesgomes.com
    expose:
      - "3001"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3001/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: https://hub.thalesgomes.com/api
    networks:
      - easypanel
      - easypanel-n8n
    expose:
      - "80"
    depends_on:
      - backend
